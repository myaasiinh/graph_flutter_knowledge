name: graph_flutter_knowledge_example
description: Demonstrates how to use the graph_flutter_knowledge package.
publish_to: 'none'

environment:
  sdk: ^3.10.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  graph_flutter_knowledge:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
